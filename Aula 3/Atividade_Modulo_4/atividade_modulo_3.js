let quantAlunos;
let cont = 1;
let contAlunos = 1;

alert("Bem-vindo ao sistema de controle de notas!");
quantAlunos = prompt("Digite a quantidade de alunos da turma:");

let aluno = [quantAlunos];

do{
    do{
        aluno = prompt("Digite o nome do aluno:");
        for(let i = 0; i < 5; i++){
            
        }
    }while(contAlunos == quantAlunos);
     









    cont++;
}while(quantAlunos == cont);